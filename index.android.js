import { AppRegistry } from 'react-native'
import App from './App'

AppRegistry.registerComponent('App', () => App)

// The below line is necessary for use with the TestBed App
AppRegistry.registerComponent('cARdViro', () => App)
