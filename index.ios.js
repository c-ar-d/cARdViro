import { AppRegistry } from 'react-native'
import Root from './Root'

AppRegistry.registerComponent('Root', () => Root)

// The below line is necessary for use with the TestBed App
AppRegistry.registerComponent('cARdViro', () => Root)
